styled = df.style.applymap(
    lambda v: "" if (pd.isna(v) or str(v).strip()=="")
              else ("color: green;" if str(v).strip().lower()=="pass" else "color: red;"),
    subset=["Status"]
)
styled.to_excel("styled_result.xlsx", index=False)